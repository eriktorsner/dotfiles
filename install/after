# Valet
if ! ($HOME/.config/composer/vendor/bin/valet | grep -q uninstall) then
	echo "Setting up Laravel valet"
	$HOME/.config/composer/vendor/bin/valet install
	$HOME/.config/composer/vendor/bin/valet tld local
fi
